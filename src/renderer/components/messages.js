import React, { Component, PropTypes } from 'react';

export default class Messages extends Component {
    constructor(...args) {
        super(...args);
     }

     render(){
         return(<h1>Test</h1>)
     }
}